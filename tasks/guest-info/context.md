# Context: Guest Information

- Guest step is step 2 (`DetailsStep`); Back returns to schedule (step 1) and Continue advances to review (step 3).
- No venue selection step exists; venue metadata stays at default values populated during initial state.
