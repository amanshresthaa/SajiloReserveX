# Context: Review & Confirm

- Review step is step 3, summarizing schedule, venue (default), and contact info.
- Submitting triggers the API call; success transitions to step 4 (confirmation) while failures show inline errors.
