- [x] Scaffold shadcn UI primitives required for navbar (`avatar`, `dropdown-menu`, `sheet`, `separator` if needed).
- [x] Implement Supabase sign-out helper for customer UI.
- [x] Build `CustomerNavbar` under `components/customer/navigation` with responsive layout and menu logic.
- [x] Replace marketing navbar usage and retire old component entry points.
- [x] Rename `/dashboard` route folder to `/my-bookings` and update internal imports.
- [x] Update all code/tests referencing `/dashboard` to `/my-bookings`.
- [x] Adjust middleware + auth flows (`redirectedFrom`, session actions, analytics) for new path.
- [x] Add unit tests for `CustomerNavbar` (unauthenticated vs authenticated).
- [x] Update existing automated tests targeting `/dashboard`.
- [x] Verify via lint/tests or targeted scripts.
